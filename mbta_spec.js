'use strict';

describe('MBTA in javascript',function(){
  it('first of all, it is a function',function(){
    expect(MBTA).toEqual(jasmine.any(Function));
    expect(MBTA).toBeDefined();
  });


  describe('green in javascript',Array(){
  it('green, it is a array contains train stop',Array(){
    expect(green).toContain(jasmine.any(Array));
    expect(MBTA).toBeDefined();
  });

  describe('red in javascript',Array(){
  it('red, it is a array contains train stop',Array(){
    expect(red).toContain(jasmine.any(Array));
    expect(red).toBeDefined();
  });

  describe('orange in javascript',Array(){
  it('orange, it is a array contains train stop',Array(){
    expect(orange).toContain(jasmine.any(Array));
    expect(orange).toBeDefined();
  });

  describe('startingLine in javascript',var(){
  it('startingLine, it is a array contains train stop',var(){
    expect(startingLine).toEqual(jasmine.any(var));
    expect(startingLine).toBeDefined();
  });

  describe('startingStation in javascript',var(){
  it('startingStation, it is a array contains train stop',var(){
    expect(startingStation).toEqual(jasmine.any(var));
    expect(startingStation).toBeDefined();
  });

  describe('endingLine in javascript',var(){
  it('endingLine, it is a array contains train stop',var(){
    expect(endingLine).toEqual(jasmine.any(var));
    expect(endingLine).toBeDefined();
  });


